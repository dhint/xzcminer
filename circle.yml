machine:
  services:
    - docker

dependencies:    
  cache_directories:
    - "~/docker"
  override:
    - docker info
    - if [[ -e ~/docker/image.tar ]]; then docker load --input ~/docker/image.tar; fi
    - docker build --rm=false -t baseboxorg/cpuminer-xzc:develop .
    - mkdir -p ~/docker; docker save baseboxorg/cpuminer-xzc:develop > ~/docker/image.tar
 
test:
  post:
    - docker run baseboxorg/cpuminer-xzc:develop --cputest
    
deployment:
  hub:
    branch: develop
    commands:
      - docker login -e $DOCKER_EMAIL -u $DOCKER_USER -p $DOCKER_PASS
      - docker push baseboxorg/cpuminer-xzc:develop
